---
'@dshline/dshline': patch
---

Clarify `/sessions` rows: delegated child sessions now carry a subdued `delegated` cue, and an untitled session that is currently open is labeled `current` instead of `untitled`.
