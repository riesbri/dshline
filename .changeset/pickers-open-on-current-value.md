---
'@dshline/dshline': patch
---

Bare `/theme`, `/model`, and `/reasoning` pickers now open on the value already
in use, so pressing Enter confirms the current selection instead of unexpectedly
choosing the first row.
