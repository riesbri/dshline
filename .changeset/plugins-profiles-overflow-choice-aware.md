---
'@dshline/dshline': patch
---

Plugins and Profiles now show `more below` only when another selectable entry
is actually outside the viewport, instead of treating selected-row detail as
another choice.
